<!doctype html>
<html lang="en">
  <head>
  </head>
  <body>
    <div class="ui segment">
      <h1>Cinema(s)</h1>
      <div class="ui two column grid container">
        <ul>
          {{#if cinemas}}
          {{#each cinemas}}
          <li>{{nome}} ({{cidade}}) <a href="/cinema/{{id}}/rooms">Rooms</a></li>
          {{/each}}
          {{else}}
          <h2>Nothing to show...</h2>
          {{/if}}
        </ul>
      </div>
    </div>
  </body>
</html>
